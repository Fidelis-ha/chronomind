// +html.tsx — Web HTML shell for expo-router
// Used only during web builds; ignored on native.
// Provides the HTML shell that loads the JS bundle.
export default function HtmlPage() {
  return null
}